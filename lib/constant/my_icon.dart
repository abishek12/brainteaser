import 'package:flutter/material.dart';

class MyIconOutlined {
  static const home = Icon(Icons.home_outlined);
  static const quiz = Icon(Icons.quiz_outlined);
  static const dashboard = Icon(Icons.dashboard_outlined);
}

class MyIcon {
  static const home = Icon(Icons.home);
  static const quiz = Icon(Icons.quiz);
  static const dashboard = Icon(Icons.dashboard);
}
